# Laravel Migrations, Eloquent ORM & CRUD Completed

The user completed Lesson 0007 (Laravel Migrations, Eloquent ORM & CRUD). They learned how to connect Laravel to MySQL, use migrations for database schema management, create Eloquent models, define relationships (hasMany, belongsTo), and perform full CRUD operations. They built a Blog CRUD API with PostController using apiResource routing.

**Status:** Active

## Evidence
- Configured .env file to connect Laravel to MySQL database
- Created and ran migrations for posts table with foreign key to users
- Created Post model with fillable properties and user() relationship
- Built PostController with full CRUD (index, store, show, update, destroy)
- Used Route::apiResource() for clean RESTful routing
- Created a seeder for test data
- Quiz answers correct on Models, where() conditions, and foreignId()->constrained()

## What this unlocks
- Ready for Lesson 0008 (Relationships & Authentication) — add comments, tags, and Sanctum auth
- The CRUD pattern will be reused throughout the rest of the course
- Glossary should add: Eloquent ORM, migration, model, seeder, CRUD, resource route
