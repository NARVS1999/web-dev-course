# Laravel Setup, Routing & Controllers Completed

The user completed Lesson 0006 (Laravel Setup, Routing & Controllers). They learned what Laravel is, installed Composer (PHP package manager), created a Laravel project, understood the directory structure, learned about routing (web.php vs api.php), HTTP methods (GET, POST, PUT, DELETE), created a controller via Artisan, and learned about Blade templates. They built a Hello World API with a GreetingController.

**Status:** Active

## Evidence
- Installed Composer and created Laravel project
- Understood the difference between web.php and api.php routes
- Created GreetingController with hello() and greet() methods
- Tested API endpoints at http://localhost:8000/api/hello
- Answered quiz correctly on composer create-project, api.php routes, and GET method

## What this unlocks
- Ready for Lesson 0007 (Migrations, Eloquent, CRUD) — Laravel is set up and connected to MySQL
- Glossary should add: Laravel, Composer, Artisan, route, controller, Blade, HTTP methods
