# MySQL & Database Basics Completed

The user completed Lesson 0005 (MySQL & Database Basics). They learned database concepts (tables, rows, columns), data types, primary/foreign keys, and SQL queries (CREATE, SELECT, INSERT, UPDATE, DELETE, JOINs). They installed XAMPP, created a `blog_db` database in phpMyAdmin, and successfully ran SQL queries.

**Status:** Active

## Evidence
- Installed XAMPP with Apache and MySQL running locally
- Created database `blog_db` with `users` and `posts` tables via phpMyAdmin
- Understood primary keys (unique identifier) vs foreign keys (table relationships)
- Answered quiz correctly on SELECT command, primary keys, and LEFT JOIN
- Inserted sample data and ran JOIN queries

## What this unlocks
- Ready for Lesson 0006 (Laravel Setup) — database foundation is laid for connecting Laravel to MySQL
- Glossary should add: database, table, row, column, SQL, primary key, foreign key, JOIN, migration
